---
title: Finance Budgets
description: Budget handlers exposed through Action Service
category: api-reference
order: 31
---

# Finance Budgets

## Supported handler

| Handler | Downstream call | Arguments |
| --- | --- | --- |
| `finance.budgets.list` | `GET /api/budgets` | `userId`, `category`, `period`, `limit`, `skip` |

## Field hints

- `entityType = budget_list`
- `collectionPath = items`
- `labelField = name`
- `valueField = limit`
- `categoryField = category`
