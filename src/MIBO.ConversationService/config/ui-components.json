{
  "components": [
    { "name": "pageTitle", "propsSchema": { "text": "string", "subtitle": "string" } },

    { "name": "searchBar", "propsSchema": { "placeholder": "string", "initialValue": "string", "actionType": "string" } },
    { "name": "categoryChips", "propsSchema": { "dataKey": "string", "selectedKey": "string", "actionType": "string" } },
    { "name": "sortDropdown", "propsSchema": { "options": "array", "selected": "string", "actionType": "string" } },

    { "name": "kpiCard", "propsSchema": { "label": "string", "valueKey": "string" } },
    { "name": "pieChart", "propsSchema": { "dataKey": "string", "title": "string" } },

    {
      "name": "dataTable",
      "propsSchema": {
        "title": "string",
        "subtitle": "string",

        "columns": "array",
        "rows": "array",

        "rowKey": "string",
        "pageSize": "number",

        "search": "object",
        "sort": "object",

        "density": "string",
        "zebra": "boolean",
        "stickyHeader": "boolean",

        "actions": "array",
        "rowAction": "object"
      }
    },

    {
      "name": "productDetail",
      "propsSchema": {
        "dataKey": "string",
        "product": "object",
        "addToCartActionType": "string",
        "userId": "number",
        "compact": "boolean"
      }
    },

    {
      "name": "carousel",
      "propsSchema": {
        "title": "string",
        "dataKey": "string",
        "itemsPath": "string",
        "itemComponent": "string",
        "itemProps": "object",
        "itemPropMap": "object",
        "cardWidthPx": "number",
        "scrollCards": "number"
      }
    },

    { "name": "pagination", "propsSchema": { "totalKey": "string", "limitKey": "string", "skipKey": "string", "actionType": "string" } },
    { "name": "cartSummary", "propsSchema": { "dataKey": "string", "checkoutActionType": "string" } }
  ]
}